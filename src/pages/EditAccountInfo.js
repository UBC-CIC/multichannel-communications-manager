import React from "react";
import { Box, Typography } from "@mui/material";

const EditAccountInfo = () => {
  return (
    <Box>
      <Typography>Edit Account Information Page</Typography>
    </Box>
  );
};

export default EditAccountInfo;
